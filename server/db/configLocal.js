const host = "localhost"
const user = "nearhome"
const password = "nearhome"
const database = "nearhome"

module.exports = {
  configInitLocal: {
    host,
    user,
    password
  },
  configLocal: {
    host,
    user,
    password,
    database
  }
}
